// API Configuration - Define backend once, use everywhere
const API_BASE = "https://shopping-bot-assd.onrender.com";

export const API_ENDPOINTS = {
  SEARCH: `${API_BASE}/api/search`,
  COMPARE: `${API_BASE}/api/compare`,
  SCRAPE: `${API_BASE}/api/scrape`,
};

export default API_BASE;
